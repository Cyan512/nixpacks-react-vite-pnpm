services:
  react-vite-pnpm:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    container_name: react-vite-pnpm
    restart: unless-stopped
    ports:
      - "4173:80"
    environment:
      - NODE_ENV=production